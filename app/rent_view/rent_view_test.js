'use strict';

describe('myApp.rent_view module', function() {

  beforeEach(module('myApp.rent_view'));

  describe('rent_view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var rentViewCtrl = $controller('rentViewCtrl');
      expect(rentViewCtrl).toBeDefined();
    }));

  });
});