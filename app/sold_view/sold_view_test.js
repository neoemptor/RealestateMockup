'use strict';

describe('myApp.sold_view module', function() {

  beforeEach(module('myApp.sold_view'));

  describe('sold_view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var soldViewCtrl = $controller('soldViewCtrl');
      expect(soldViewCtrl).toBeDefined();
    }));

  });
});