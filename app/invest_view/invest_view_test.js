'use strict';

describe('myApp.invest_view module', function() {

  beforeEach(module('myApp.invest_view'));

  describe('invest_view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var investViewCtrl = $controller('investViewCtrl');
      expect(investViewCtrl).toBeDefined();
    }));

  });
});