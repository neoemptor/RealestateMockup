'use strict';

describe('myApp.share_view module', function() {

  beforeEach(module('myApp.share_view'));

  describe('share_view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var shareViewCtrl = $controller('shareViewCtrl');
      expect(shareViewCtrl).toBeDefined();
    }));

  });
});