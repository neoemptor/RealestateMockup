'use strict';

describe('myApp.buy_view module', function() {

  beforeEach(module('myApp.buy_view'));

  describe('buy_view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var buyViewCtrl = $controller('buyViewCtrl');
      expect(buyViewCtrl).toBeDefined();
    }));

  });
});